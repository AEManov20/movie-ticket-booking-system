import 'package:flutter/material.dart';

class SelfPage extends StatelessWidget {
  const SelfPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Self page"));
  }
}